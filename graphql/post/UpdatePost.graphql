mutation UpdatePost($input: updatePostInput) {
  updatePost(input: $input) {
    post {
      id
      slug
      title
      description
      content
      contentType
      displayType
      category {
        id
        name
      }
    }
  }
}
