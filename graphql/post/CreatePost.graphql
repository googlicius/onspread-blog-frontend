mutation CreatePost($input: createPostInput) {
  createPost(input: $input) {
    post {
      id
      slug
      title
      description
      content
      contentType
      displayType
      category {
        id
        name
      }
    }
  }
}
