mutation UpdatePost($input: updatePostInput) {
  updatePost(input: $input) {
    post {
      id
      title
      description
      content
      contentType
      displayType
      category {
        id
        name
      }
    }
  }
}
