mutation CreateStory($input: createStoryInput!) {
  createStory(input: $input) {
    story {
      id
      name
      description
      image {
        url
        formats
        provider
      }
    }
  }
}
