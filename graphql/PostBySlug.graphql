query PostBySlug($slug: String!) {
  postBySlug(slug: $slug) {
    id
    title
    description
    content
    contentType
    heart
    image {
      url
      provider
      formats
    }
    user {
      id
      username
    }
  }
}
