query PostBySlug($slug: String!) {
  postBySlug(slug: $slug) {
    id
    slug
    title
    description
    content
    contentType
    heart
    published_at
    image {
      url
      provider
      formats
    }
    user {
      id
      username
    }
  }
}
