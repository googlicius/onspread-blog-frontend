mutation Login($input: UsersPermissionsLoginInput!) {
  login(input: $input) {
    user {
      id
      username
      email
      avatar {
        url
        formats
        alternativeText
      }
    }
  }
}
